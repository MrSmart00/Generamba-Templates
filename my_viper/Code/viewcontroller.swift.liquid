{% include 'header' %}

import Foundation
import UIKit
import Combine
import Common

final class {{ module_info.name }}ViewController: UIViewController, {{ module_info.name }}View, Injectable {
    struct Dependency {
        let presenter: {{ module_info.name }}Presentation
    }

    private var dependency: Dependency!

    private var cancellables = Set<AnyCancellable>()

    override func viewDidLoad() {
        super.viewDidLoad()
    }

    func inject(dependency: {{ module_info.name }}ViewController.Dependency) {
        self.dependency = dependency
    }
}
